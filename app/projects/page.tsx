import type { Metadata } from "next";import SectionPage from "@/components/section-page";import { pageContent } from "@/data/site";export const metadata:Metadata={title:"Solar Project Applications",description:"Explore Solarious module application categories for rooftop, industrial and utility solar projects."};export default function Page(){return <SectionPage content={pageContent.projects} image="/utility-solar.webp"/>}
