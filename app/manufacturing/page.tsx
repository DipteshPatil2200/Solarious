import type { Metadata } from "next";import SectionPage from "@/components/section-page";import { manufacturingSteps,pageContent } from "@/data/site";export const metadata:Metadata={title:"Solar Module Manufacturing",description:"Explore the Solarious solar module manufacturing process, automation, testing and traceability."};export default function Page(){return <SectionPage content={pageContent.manufacturing} image="/manufacturing-line.webp" extra={<div className="process-block"><p className="eyebrow">How we build excellence</p><h2>Manufacturing process</h2><div>{manufacturingSteps.map((step,i)=><article key={step}><span>{String(i+1).padStart(2,'0')}</span><strong>{step}</strong></article>)}</div></div>}/>}
