import type { Metadata } from "next";
import Link from "next/link";
import ToolPageShell from "@/components/tool-page-shell";
export const metadata: Metadata = { title: "Compare Solar Module Technologies", description: "Compare Mono PERC, TOPCon, HJT and Bifacial solar module categories." };
const rows=[{name:"Cell approach",values:["PERC passivation","Tunnel oxide contact","Heterojunction layers","Front + rear generation"]},{name:"Typical fit",values:["Commercial and industrial","Performance-focused projects","Space-constrained projects","Suitable ground-mount designs"]},{name:"Bifacial capability",values:["Configuration dependent","Configuration dependent","Configuration dependent","Core architecture"]},{name:"Project review",values:["Required","Required","Required","Albedo + mounting review"]}];
export default function Page(){return <ToolPageShell eyebrow="Approved category guidance" title="Compare Module Technologies" intro="A plain-language category comparison. Confirm every product-specific value with the Solarious technical team."><section className="compare-wrap"><div className="compare-table" role="table" aria-label="Solar module technology comparison"><div className="compare-row compare-head" role="row"><span role="columnheader">Criteria</span>{["Mono PERC","TOPCon","HJT","Bifacial"].map(x=><strong role="columnheader" key={x}>{x}</strong>)}</div>{rows.map(row=><div className="compare-row" role="row" key={row.name}><span role="rowheader">{row.name}</span>{row.values.map((v,i)=><p role="cell" key={`${row.name}-${i}`}>{v}</p>)}</div>)}</div><div className="compare-cta"><p>No unverified efficiency, power or warranty values are displayed.</p><Link className="button" href="/contact#quote">Request confirmed specifications</Link></div></section></ToolPageShell>}
