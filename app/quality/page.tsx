import type { Metadata } from "next";import SectionPage from "@/components/section-page";import { pageContent } from "@/data/site";export const metadata:Metadata={title:"Quality & Certifications",description:"Discover the inspection, testing and traceability processes behind Solarious module quality."};export default function Page(){return <SectionPage content={pageContent.quality} image="/manufacturing-line.webp"/>}
