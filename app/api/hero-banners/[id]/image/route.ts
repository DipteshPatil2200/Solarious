import { runtimeEnv as env } from "@/lib/runtime-env";import { NextResponse } from "next/server";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const row=await env.DB.prepare("SELECT image_key FROM hero_banners WHERE id=? AND published=1").bind(Number(id)).first<{image_key:string|null}>();if(!row?.image_key)return NextResponse.json({error:"Image not found"},{status:404});const object=await env.BUCKET.get(row.image_key);if(!object)return NextResponse.json({error:"Image not found"},{status:404});return new Response(object.body,{headers:{"Content-Type":object.httpMetadata?.contentType??"image/webp","Cache-Control":"public, max-age=3600"}})}
