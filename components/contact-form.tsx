"use client";
import { useState } from "react";
import { Send } from "lucide-react";
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
import { products } from "@/data/site";
export default function ContactForm(){const [sent,setSent]=useState(false);if(sent)return <div className="form-success"><strong>Inquiry received.</strong><p>Thank you. Your details are ready for the Solarious team workflow. Production delivery requires a configured backend notification service.</p><button className="button button-ghost" onClick={()=>setSent(false)}>Send another inquiry</button></div>;return <form className="lead-form" onSubmit={e=>{e.preventDefault();setSent(true)}}><label>Full name *<input name="name" required autoComplete="name"/></label><label>Company name *<input name="company" required autoComplete="organization"/></label><label>Email address *<input type="email" name="email" required autoComplete="email"/></label><label>Phone number *<input type="tel" name="phone" required autoComplete="tel"/></label><label>Product interest<Select name="product"><SelectTrigger className="tool-select"><SelectValue placeholder="Select technology"/></SelectTrigger><SelectContent>{products.map(p=><SelectItem key={p.slug} value={p.slug}>{p.name}</SelectItem>)}</SelectContent></Select></label><label>Project capacity<input name="capacity" placeholder="e.g. 500 kW"/></label><label className="full">Project location *<input name="location" required/></label><label className="full">Message *<textarea name="message" required rows={5}/></label><button className="button full" type="submit">Submit inquiry <Send size={16}/></button></form>}
