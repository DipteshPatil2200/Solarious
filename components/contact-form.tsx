"use client";
import { useState } from "react";
import { Loader2, Send } from "lucide-react";
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
import { products } from "@/data/site";
export default function ContactForm(){const [sent,setSent]=useState(false);const [sending,setSending]=useState(false);const [error,setError]=useState("");const [product,setProduct]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSending(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/inquiries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...data,product,kind:"contact"})});setSending(false);if(r.ok)setSent(true);else setError((await r.json()).error??"Submission failed.");}if(sent)return <div className="form-success"><strong>Inquiry received.</strong><p>Thank you. Your enquiry has been saved and is now available to the Solarious team.</p><button className="button button-ghost" onClick={()=>setSent(false)}>Send another inquiry</button></div>;return <form className="lead-form" onSubmit={submit}><label>Full name *<input name="name" required autoComplete="name"/></label><label>Company name *<input name="company" required autoComplete="organization"/></label><label>Email address *<input type="email" name="email" required autoComplete="email"/></label><label>Phone number *<input type="tel" name="phone" required autoComplete="tel"/></label><label>Product interest<Select value={product} onValueChange={setProduct}><SelectTrigger className="tool-select"><SelectValue placeholder="Select technology"/></SelectTrigger><SelectContent>{products.map(p=><SelectItem key={p.slug} value={p.slug}>{p.name}</SelectItem>)}</SelectContent></Select></label><label>Project capacity<input name="capacity" placeholder="e.g. 500 kW"/></label><label className="full">Project location *<input name="location" required/></label><label className="full">Message *<textarea name="message" required rows={5}/></label>{error&&<p className="form-error full">{error}</p>}<button className="button full" type="submit" disabled={sending}>{sending?<Loader2 className="spin" size={16}/>:<Send size={16}/>} {sending?"Submitting…":"Submit inquiry"}</button></form>}
