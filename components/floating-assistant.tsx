"use client";
import { useState } from "react";
import Link from "next/link";
import { Bot, Send } from "lucide-react";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
export default function FloatingAssistant(){const [q,setQ]=useState("");const [a,setA]=useState("Ask about module technologies, product selection, manufacturing or quotations.");const answer=(value:string)=>{const x=value.toLowerCase();if(x.includes("topcon"))setA("TOPCon is a performance-focused technology category. Confirm exact Solarious efficiency, power and warranty values with the technical team.");else if(x.includes("bifacial"))setA("Bifacial suitability depends on rear-side irradiance, mounting and site albedo. A technical review is recommended.");else if(x.includes("quote"))setA("Use the Request Quote form and include your project capacity, location, timeline and preferred technology.");else setA("Please contact the Solarious technical team for confirmed information.");setQ("")};return <Dialog><DialogTrigger asChild><button className="floating-ai"><Bot size={19}/><span>Ask Solarious AI</span></button></DialogTrigger><DialogContent className="assistant-dialog"><DialogHeader><DialogTitle>Solarious AI Assistant</DialogTitle><DialogDescription>Deterministic guidance from approved website content. Product-specific claims require technical confirmation.</DialogDescription></DialogHeader><div className="assistant-answer" aria-live="polite">{a}</div><form className="assistant-form" onSubmit={e=>{e.preventDefault();if(q.trim())answer(q)}}><label className="sr-only" htmlFor="global-ai">Ask a question</label><input id="global-ai" value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask about modules or projects…"/><button aria-label="Send" type="submit"><Send size={18}/></button></form><div className="assistant-links"><Link href="/module-recommender">Find a Module</Link><Link href="/compare">Compare Products</Link><Link href="/quote">Request Quote</Link><Link href="/contact">Contact Sales</Link></div></DialogContent></Dialog>}
