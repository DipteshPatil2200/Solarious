import Image from "next/image";import Link from "next/link";import {ChevronRight} from "lucide-react";
export default function InnerPageHero({eyebrow,title,description,image,breadcrumb,actions}:{eyebrow:string;title:string;description:string;image?:string;breadcrumb?:string;actions?:React.ReactNode}){return <section className={`premium-inner-hero ${image?"with-image":""}`}><div className="inner-hero-glow" aria-hidden="true"/><div className="premium-inner-copy"><nav className="hero-breadcrumb" aria-label="Breadcrumb"><Link href="/">Home</Link><ChevronRight size={14}/><span aria-current="page">{breadcrumb??title}</span></nav><p className="eyebrow"><span/> {eyebrow}</p><h1>{title}</h1><p>{description}</p>{actions&&<div className="hero-actions">{actions}</div>}</div>{image&&<div className="premium-inner-image"><Image src={image} alt="" fill priority sizes="(max-width: 900px) 100vw, 44vw"/><span aria-hidden="true"/></div>}</section>}
