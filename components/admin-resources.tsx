"use client";import { useCallback,useEffect,useMemo,useState } from "react";import { Download,FilePlus2,Loader2,Pencil,Search,Trash2,X } from "lucide-react";
type Resource={id:number;title:string;description:string;category:string;product:string;file_name:string;file_type:string;file_size:number;published:number;display_order:number;updated_at:string};
export default function AdminResources(){const [items,setItems]=useState<Resource[]>([]),[categories,setCategories]=useState<string[]>([]),[query,setQuery]=useState(""),[editing,setEditing]=useState<Resource|null|undefined>(undefined),[busy,setBusy]=useState(false),[notice,setNotice]=useState("");const load=useCallback(()=>fetch("/api/admin/resources").then(r=>r.json()).then(d=>{setItems(d.resources??[]);setCategories(d.categories??[])}) ,[]);useEffect(()=>{void load()},[load]);const filtered=useMemo(()=>items.filter(x=>`${x.title} ${x.category} ${x.product}`.toLowerCase().includes(query.toLowerCase())),[items,query]);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setNotice("");const data=new FormData(e.currentTarget);if(editing?.id)data.set("id",String(editing.id));const r=await fetch("/api/admin/resources",{method:"POST",body:data});const out=await r.json();setBusy(false);if(r.ok){setNotice("Resource saved successfully.");setEditing(undefined);await load()}else setNotice(out.error??"Resource could not be saved.")}async function toggle(x:Resource){const r=await fetch("/api/admin/resources",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:x.id,published:!x.published})});if(r.ok)await load()}async function remove(x:Resource){if(!confirm(`Delete ${x.title} and its stored file?`))return;const r=await fetch(`/api/admin/resources?id=${x.id}`,{method:"DELETE"});if(r.ok){setNotice("Resource deleted.");await load()}}return <section className="admin-panel"><div className="admin-panel-head"><div><h2>Resources & Downloads</h2><p>Upload, publish and manage customer-facing documents.</p></div><button className="button" onClick={()=>setEditing(null)}><FilePlus2 size={16}/> Add resource</button></div>{notice&&<div className="admin-notice">{notice}</div>}<div className="resource-admin-search"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search resources"/></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Document</th><th>Category</th><th>File</th><th>Order</th><th>Published</th><th>Actions</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><strong>{x.title}</strong><small>{x.description}</small></td><td>{x.category}<small>{x.product}</small></td><td><a href={`/api/resources/${x.id}/file`} target="_blank" rel="noreferrer">{x.file_name}</a><small>{(x.file_size/1024/1024).toFixed(1)} MB</small></td><td>{x.display_order}</td><td><button className={`publish-toggle ${x.published?"on":""}`} onClick={()=>void toggle(x)}>{x.published?"Published":"Draft"}</button></td><td><div className="table-actions"><button className="icon-button" onClick={()=>setEditing(x)} aria-label="Edit"><Pencil size={15}/></button><a className="icon-button" href={`/api/resources/${x.id}/file?download=1`} aria-label="Download"><Download size={15}/></a><button className="icon-button danger" onClick={()=>void remove(x)} aria-label="Delete"><Trash2 size={15}/></button></div></td></tr>)}</tbody></table>{!filtered.length&&<div className="admin-empty">No resources found.</div>}</div>{editing!==undefined&&<div className="admin-modal-backdrop" onMouseDown={()=>setEditing(undefined)}><div className="admin-modal" onMouseDown={e=>e.stopPropagation()} role="dialog" aria-modal="true"><button className="modal-close" onClick={()=>setEditing(undefined)} aria-label="Close"><X/></button><h2>{editing?"Edit resource":"Add resource"}</h2><form onSubmit={save}><label>Title *<input name="title" required defaultValue={editing?.title}/></label><label>Short description<textarea name="description" rows={3} defaultValue={editing?.description}/></label><label>Category *<select name="category" required defaultValue={editing?.category??""}><option value="">Select category</option>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label>Related product<input name="product" defaultValue={editing?.product} placeholder="Optional"/></label><label>Display order<input name="displayOrder" type="number" defaultValue={editing?.display_order??0}/></label><label>Document {editing?"(choose only to replace)":"*"}<input name="file" type="file" required={!editing} accept=".pdf,.doc,.docx,.xls,.xlsx"/></label><label className="checkbox-label"><input name="published" type="checkbox" value="true" defaultChecked={Boolean(editing?.published)}/> Publish immediately</label><p className="assumption">PDF, DOC, DOCX, XLS or XLSX; maximum 20 MB. Files are stored in persistent cloud storage.</p><div className="admin-modal-actions"><button type="button" className="button button-ghost" onClick={()=>setEditing(undefined)}>Cancel</button><button className="button" disabled={busy}>{busy?<Loader2 className="spin" size={16}/>:null}{busy?"Uploading…":"Save resource"}</button></div></form></div></div>}</section>}
